:root{--bg:#0A111E;--surf:#121D31;--surf2:#16233A;--surf3:#0E1830;--line:#24344F;--line2:#1B2942;
--gold:#D9A441;--gold-dim:#8A6A2A;--text:#E9EEF7;--muted:#93A3BE;--faint:#63758F;
--up:#FF5B6B;--down:#4E9CFF;--flat:#8FA0BC;--s1:#B8862C;--s2:#5A8FD4;--ok:#4FB286;
--mono:"IBM Plex Mono",ui-monospace,Menlo,monospace;--sans:"Noto Sans KR",system-ui,-apple-system,sans-serif;}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--text);font-family:var(--sans);font-size:14px;line-height:1.55;-webkit-font-smoothing:antialiased}
.wrap{max-width:1240px;margin:0 auto;padding:38px 26px 64px;display:flex;flex-direction:column;gap:30px}
.eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.22em;color:var(--gold);text-transform:uppercase;font-weight:600}
h1{font-size:27px;font-weight:700;margin:8px 0 0;letter-spacing:-.02em;text-wrap:balance}
.meta{color:var(--muted);font-size:12.5px;margin-top:10px}.meta b{color:var(--text);font-weight:500}
.head{border-bottom:2px solid var(--gold);padding-bottom:20px}
.notice{margin-top:12px;color:#EED9A8;font-size:12.5px}
.kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(168px,1fr));gap:12px}
.kpi{background:var(--surf);border:1px solid var(--line);border-radius:5px;padding:14px 15px;display:flex;flex-direction:column;gap:5px}
.kpi .k{font-size:11.5px;color:var(--muted)}
.kpi .v{font-family:var(--mono);font-size:24px;font-weight:600;letter-spacing:-.02em;font-variant-numeric:tabular-nums}
.kpi .s{font-size:11px;color:var(--faint)}
h2{font-size:16px;margin:0 0 14px;padding-left:11px;border-left:3px solid var(--gold);line-height:1.25}
.scroll{overflow-x:auto;border:1px solid var(--line);border-radius:5px;background:var(--surf)}
table{width:100%;border-collapse:collapse;font-size:12.5px;white-space:nowrap}
th{background:var(--surf2);color:var(--muted);font-weight:500;font-size:11.5px;text-align:left;padding:10px 12px;border-bottom:1px solid var(--line);position:sticky;top:0}
td{padding:9px 12px;border-bottom:1px solid var(--line2);vertical-align:middle}
tr:last-child td{border-bottom:none}tr.grp td{border-top:2px solid var(--line)}
tbody tr[data-key]{cursor:pointer}
tbody tr[data-key]:hover{background:#16243C}
tbody tr[data-key]:focus-visible{outline:2px solid var(--gold);outline-offset:-2px}
th.n,td.n{text-align:right;font-family:var(--mono);font-variant-numeric:tabular-nums}
.tk{font-family:var(--mono);color:#A9C4E8;letter-spacing:.04em}
.mk{color:var(--muted);font-size:11.5px}.dim{color:var(--faint)}
.fl{color:var(--gold);font-size:9px;margin-left:2px}
.gold{color:var(--gold)}.tot{font-weight:600}
.up{color:var(--up)}.down{color:var(--down)}.flat{color:var(--flat)}
.pill{font-size:10px;padding:1.5px 6px;border-radius:3px;margin-left:6px;font-family:var(--mono);letter-spacing:.04em}
.pill.trend{background:rgba(217,164,65,.14);color:var(--gold);border:1px solid var(--gold-dim)}
.pill.rev{background:rgba(78,156,255,.13);color:var(--down);border:1px solid #2C5A94}
td.sc{position:relative;min-width:74px}
td.sc i{position:absolute;left:8px;top:50%;transform:translateY(-50%);height:16px;background:linear-gradient(90deg,rgba(217,164,65,.30),rgba(217,164,65,.10));border-radius:2px;max-width:calc(100% - 34px)}
td.sc b{position:relative;font-weight:600}
td.spk{width:78px;padding:6px 12px}td.spk svg{display:block}
.chartbox{background:var(--surf);border:1px solid var(--line);border-radius:5px;padding:18px}
.legend{display:flex;gap:18px;align-items:center;font-size:12px;color:var(--muted);margin-bottom:12px}
.legend i{display:inline-block;width:14px;height:2px;margin-right:7px;vertical-align:middle;border-radius:1px}
.empty{color:var(--faint);font-size:12.5px;text-align:center;padding:44px 10px;border:1px dashed var(--line);border-radius:4px}
.note{background:var(--surf);border:1px solid var(--line);border-left:3px solid var(--gold);border-radius:5px;padding:16px 18px}
.note h3{margin:0 0 9px;font-size:13.5px;color:var(--gold);font-weight:600}
.note ul{margin:0;padding-left:17px;color:#C7D3E6;font-size:12.5px}.note li{margin:5px 0}.note li b{color:var(--text);font-weight:500}
.cap{color:var(--faint);font-size:11.5px;margin-top:9px}
footer{border-top:1px solid var(--line);padding-top:16px;color:var(--faint);font-size:11.5px;display:flex;flex-wrap:wrap;gap:6px 22px}
#tip{position:fixed;pointer-events:none;background:#0d1626;border:1px solid var(--line);border-radius:4px;padding:8px 10px;font-size:11.5px;font-family:var(--mono);opacity:0;transition:opacity .12s;z-index:60}
#scrim{position:fixed;inset:0;background:rgba(4,8,16,.66);opacity:0;pointer-events:none;transition:opacity .18s;z-index:40}
#scrim.on{opacity:1;pointer-events:auto}
#dw{position:fixed;top:0;right:0;bottom:0;width:min(720px,100%);background:var(--surf3);border-left:1px solid var(--line);
 transform:translateX(100%);transition:transform .22s ease;z-index:50;display:flex;flex-direction:column;overflow:hidden}
#dw.on{transform:none}
.dwh{padding:20px 24px 0;border-bottom:1px solid var(--line);flex:0 0 auto}
.dwh .r1{display:flex;align-items:baseline;gap:10px;flex-wrap:wrap}
.dwh h3{margin:0;font-size:19px;font-weight:700}
.dwh .sub{color:var(--muted);font-size:12px;font-family:var(--mono)}
.dwh .px{margin:10px 0 4px;display:flex;gap:22px;flex-wrap:wrap;align-items:baseline}
.dwh .px b{font-family:var(--mono);font-size:25px;font-weight:600;font-variant-numeric:tabular-nums}
.dwh .px span{font-size:12px;color:var(--muted)}
.tabs{display:flex;gap:2px;margin-top:14px}
.tabs button{background:none;border:none;border-bottom:2px solid transparent;color:var(--muted);font-family:var(--sans);
 font-size:13px;padding:8px 14px;cursor:pointer}
.tabs button[aria-selected="true"]{color:var(--gold);border-bottom-color:var(--gold)}
.tabs button:focus-visible{outline:2px solid var(--gold);outline-offset:-2px}
.dwb{padding:20px 24px 40px;overflow-y:auto;flex:1 1 auto;display:flex;flex-direction:column;gap:20px}
.close{position:absolute;top:16px;right:18px;background:none;border:1px solid var(--line);color:var(--muted);
 width:30px;height:30px;border-radius:4px;cursor:pointer;font-size:15px;line-height:1}
.close:hover{color:var(--text);border-color:var(--faint)}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(112px,1fr));gap:9px}
.cell{background:var(--surf);border:1px solid var(--line);border-radius:4px;padding:9px 11px}
.cell .k{font-size:10.5px;color:var(--muted);white-space:nowrap}
.cell .v{font-family:var(--mono);font-size:15px;font-weight:600;margin-top:2px;font-variant-numeric:tabular-nums}
.blk h4{margin:0 0 9px;font-size:12px;color:var(--gold);font-weight:600;letter-spacing:.04em}
.rng{background:var(--surf);border:1px solid var(--line);border-radius:4px;padding:12px 13px}
.rngbar{position:relative;height:6px;background:#1D2B45;border-radius:3px;margin:12px 0 7px}
.rngbar i{position:absolute;top:-3px;width:2px;height:12px;background:var(--gold);border-radius:1px}
.rngbar u{position:absolute;top:0;height:6px;background:linear-gradient(90deg,rgba(90,143,212,.35),rgba(184,134,44,.5));border-radius:3px}
.rngl{display:flex;justify-content:space-between;font-family:var(--mono);font-size:10.5px;color:var(--faint)}
.bar{height:5px;background:#1D2B45;border-radius:3px;overflow:hidden;margin-top:6px}
.bar i{display:block;height:100%;background:var(--gold);border-radius:3px}
table.fin{width:100%;border-collapse:collapse;font-size:12px}
table.fin th{position:static;background:var(--surf2);font-size:11px;padding:7px 9px}
table.fin td{padding:7px 9px;font-family:var(--mono);text-align:right;font-variant-numeric:tabular-nums}
table.fin td:first-child,table.fin th:first-child{text-align:left;font-family:var(--sans)}
.seg{display:inline-flex;border:1px solid var(--line);border-radius:4px;overflow:hidden;margin-bottom:10px}
.seg button{background:var(--surf);border:none;color:var(--muted);font-family:var(--sans);font-size:11.5px;padding:5px 13px;cursor:pointer}
.seg button[aria-pressed="true"]{background:var(--gold);color:#121A28;font-weight:500}
.nw{display:flex;flex-direction:column;gap:9px}
.nwi{background:var(--surf);border:1px solid var(--line);border-radius:4px;padding:11px 13px}
.nwi .m{display:flex;gap:9px;align-items:center;margin-bottom:5px}
.nwi .d{font-family:var(--mono);font-size:10.5px;color:var(--faint)}
.nwi .kd{font-size:10px;padding:1px 6px;border-radius:3px;border:1px solid var(--line)}
.kd.실적{background:rgba(217,164,65,.15);color:var(--gold);border-color:var(--gold-dim)}
.kd.리포트{background:rgba(78,156,255,.13);color:var(--down);border-color:#2C5A94}
.kd.뉴스{background:#1C2A44;color:var(--muted)}
.nwi p{margin:0;font-size:12.5px;color:#D6DFEE;line-height:1.5}
.nwi a{color:var(--faint);font-size:11px;text-decoration:none;display:inline-block;margin-top:6px}
.nwi a:hover{color:var(--gold);text-decoration:underline}
.hint{color:var(--faint);font-size:11.5px}
@media(max-width:640px){h1{font-size:21px}.wrap{padding:26px 15px 48px}.kpi .v{font-size:20px}.dwb{padding:16px 16px 40px}.dwh{padding:16px 16px 0}}
@media(prefers-reduced-motion:reduce){*{transition:none!important}}
